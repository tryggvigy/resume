'use strict';

ExperienceCtrl.$inject = ['$scope', '$location', 'languageFactory'];

function ExperienceCtrl($scope, $location, languageFactory) {
  var vm = this;
  //This page is just one big directive.
}
